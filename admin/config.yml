# config.yml

backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: "assets/uploads"
collections:
  - name: "project"
    label: "Project"
    folder: "_projects/"
    create: true
    editor:
      preview: false
    fields:
      - { name: Title }
